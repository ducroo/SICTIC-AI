# people_ranking

**Description:**
Core engine to rank SICTIC members against a startup profile using pairwise comparisons. This acts as the unified backend for skills like `expert_search` and `potential_investors`.

**Internal Usage Only:**
This skill is designed to be imported programmatically as a library function by other skills. It does not provide a standalone user-facing entry point.
