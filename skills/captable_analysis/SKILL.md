---
name: captable_analysis
description: Compatibility alias for captable; use the canonical captable skill for capitalization reports.
---

# captable_analysis

Temporary command/Python alias for [captable](../captable/SKILL.md).
It forwards to the same implementation and creates no separate artifacts.
Use `/captable` or `python -m skills.captable run --startup example`.
The old `render` command and snapshot-selection `--as-of` are removed.
