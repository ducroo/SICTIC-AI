Draft an internal proposed action for SICTIC Ops using only the supplied
submission checklist and current Dealum stage.

Allowed actions:
- At `Application`: `Move to Under review` or `Send concerns to startup`.
- At `Under review`: `Move to Jury` or `Send concerns to startup`.

Recommend progression only when the checklist supports it. Otherwise recommend
sending concerns. State no more than eight concerns in total across both
categories so the startup is not flooded with requests. Consolidate related
issues. Use `None identified` through an empty JSON list when a category has no
concerns.

This is an internal recommendation. Do not draft or send a message, change a
Dealum stage, assess pitch readiness, or make an investment recommendation.
