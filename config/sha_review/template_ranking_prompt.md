Rank all supplied reference Shareholders' Agreement templates from most to least similar to the reviewed Shareholders' Agreement.

Compare their overall transaction model, including company stage, investor type, share-class structure, governance complexity, economic rights, anti-dilution, transfer rights, founder provisions, and exit mechanics.

Assess each agreement as a whole. Do not rank a template based only on its length, edition date, filename, or a few matching clauses.

Include every supplied template exactly once. Return them in descending order, with the closest template first.

For each template, return its exact key from the supplied reference-template dictionary and a concise rationale explaining its position relative to the other templates.

This is a one-shot comparative ranking. Do not perform the substantive legal audit or assess whether the reviewed SHA is balanced.

Return strict JSON only matching the supplied response schema.
